# SDK content hash scope — paths included in the SDK cache key.
#
# Each line is a file or directory (trailing /) relative to the azure-container-linux
# repo root. Consumed by compute_sdk_hash.sh and test_sdk_hash.sh.
#
# See acl-pipelines docs/rfcs/sdk-container-caching.md for rationale.
#
# Directories:
sdk_container/
sdk_lib/
acl/sdk-cache/
# Individual files:
acl/MICROSOFT-RPM-GPG-KEY
build_library/toolchain_util.sh
build_library/update_chroot_util.sh
build_library/set_lsb_release
