https://src.fedoraproject.org/rpms/grub2/pull-request/238#comment-330064
https://src.fedoraproject.org/rpms/grub2/c/a75b37ecffefeee9e61f580b8d78ca4a545bc502?branch=f44

From 9313f41d03c65b7e3407140ceef5d5dcb5633e1f Mon Sep 17 00:00:00 2001
From: James Le Cuirot <jlecuirot@microsoft.com>
Date: Mon, 29 Jun 2026 15:04:06 +0100
Subject: [PATCH] Revert "verifiers: Allocate EFI pages instead of grub_malloc
 for verified buffer"

This reverts commit f3330b6c42690f4201ac006f8f8968a727929ab7.
--- a/grub-core/kern/verifiers.c
+++ b/grub-core/kern/verifiers.c
@@ -22,10 +22,6 @@
 #include <grub/verify.h>
 #include <grub/dl.h>
 
-#if defined (GRUB_MACHINE_EFI) && defined (__x86_64__)
-#include <grub/efi/efi.h>
-#endif
-
 GRUB_MOD_LICENSE ("GPLv3+");
 
 struct grub_file_verifier *grub_file_verifiers;
@@ -42,13 +38,7 @@ verified_free (grub_verified_t verified)
 {
   if (verified)
     {
-#if defined (GRUB_MACHINE_EFI) && defined (__x86_64__)
-      if (verified->buf && verified->file->size)
-	grub_efi_free_pages ((grub_efi_physical_address_t)(grub_addr_t)verified->buf,
-			     GRUB_EFI_BYTES_TO_PAGES(verified->file->size));
-#else
       grub_free (verified->buf);
-#endif
       grub_free (verified);
     }
 }
@@ -155,11 +145,7 @@ grub_verifiers_open (grub_file_t io, enum grub_file_type type)
     {
       goto fail;
     }
-#if defined (GRUB_MACHINE_EFI) && defined (__x86_64__)
-  verified->buf = grub_efi_allocate_any_pages (GRUB_EFI_BYTES_TO_PAGES (ret->size));
-#else
   verified->buf = grub_malloc (ret->size);
-#endif
   if (!verified->buf)
     {
       goto fail;
-- 
2.54.0

